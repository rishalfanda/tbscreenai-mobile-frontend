# Perbaikan F01–F03 — 10 September 2026

Status: implementasi lokal selesai; bukan persetujuan release klinis. Tidak ada commit/push atau perubahan backend.

## Perubahan

1. **F01, atribusi hasil:** hasil mengikat snapshot draft dan gambar yang dianalisis. Edit draft/reset/logout menginvalidasi hasil; completion request lama diabaikan. ResultScreen membaca snapshot, bukan draft aktif. Byte gambar tidak dapat dimutasi dari luar.
2. **F02, isolasi sesi:** logout membersihkan state diagnosis serta cache, antrean, dan settings lokal. Pergantian sesi menginvalidasi request dan write async lama. Login berhasil memeriksa pemilik cache: akun sama mempertahankan antrean, akun berbeda membersihkan data lama. Login gagal tidak menghapus antrean offline. Restore hanya menerima token belum kedaluwarsa dengan owner sesuai; validasi ini bukan verifikasi signature JWT.
3. **F03, hasil simulasi:** kontrak HTTP mempertahankan `is_mock`; nilai hilang dianggap mock. Hasil mock diberi label DUMMY/DEMO dan aplikasi selalu menampilkan banner DEMO. `APP_ENV` default `demo`, hanya `demo`/`staging` diperbolehkan; `APP_ENV=production` sengaja gagal kompilasi selama fitur klinis simulasi masih ada. `USE_HTTP=true` tidak berarti aplikasi siap klinis.

Preview hasil sekarang menggunakan gambar snapshot, tetapi ini tidak menutup seluruh F04 atau temuan lainnya.

## Verifikasi

- `flutter analyze --no-pub`: PASS, no issues.
- `flutter build apk --debug --no-pub`: PASS; APK terbaru di `build/app/outputs/flutter-apk/app-debug.apk`.
- `dart run build_runner build`: PASS; generated Drift tidak menghasilkan diff.
- `flutter test --no-pub --reporter expanded`: PASS, **65 test** (50 existing + 15 test baru).
- `test/p0_safety_test.dart`: 15 PASS; mencakup snapshot/immutability, reset/dispose, stale refresh/pull/login, owner cache, kegagalan login, restore token, flag mock, pergantian akun, dan render hasil demo.
- Audit regresi historis setelah patch: 6 PASS / 9 FAIL; enam kasus terkait P0 membaik, sembilan kegagalan lain tetap terbuka. File audit di luar `test/` memang mereproduksi backlog dan tidak termasuk suite default.
- `flutter build bundle --no-pub --dart-define=APP_ENV=production`: gagal sesuai guard yang diharapkan, bukan kegagalan build demo.
- `git diff --check`: PASS (peringatan konversi LF/CRLF Windows saja).

## Batasan dan dampak operasional

- Tidak diuji dengan backend live, perangkat fisik, atau data pasien nyata. Test memakai database in-memory dan respons HTTP buatan.
- Schema Drift tetap versi 1: owner menggunakan settings yang sudah ada, tanpa migrasi tabel.
- Instalasi lama tanpa `session_owner` wajib login ulang. Data tanpa owner tidak dapat diatribusikan aman dan dibersihkan setelah login berhasil.
- Logout eksplisit menghapus cache dan antrean lokal, termasuk data belum tersinkron; sinkronkan sebelum logout. Login gagal tidak melakukan penghapusan ini.
- F05–F13 dan bagian F04 yang belum disentuh tetap backlog, termasuk konflik/retry sync dan token plaintext. Jangan menganggap hasil test hijau sebagai kesiapan produksi.
